# Portable-Powershell-Html-Launcher
A portable single-file Windows batch script that launches a WinForms-based Lightweight HTML/URL viewer using PowerShell and the IE11 WebBrowser control. Zero dependencies — runs on Windows 7+.
